# MoodCopilot Agent Guide

## 产品边界

- `frontend/` 是网页端，包含完整社区能力。
- `frontend-uniapp/` 是微信小程序，定位为私密的日记、AI 陪伴与个人成长工具。
- 小程序**不提供**广场、关注、用户主页、评论、点赞/共鸣、举报或其他公开社交入口。
- 不要因为网页端存在这些页面或后端已有相应接口，就在小程序添加路由、入口、互动按钮或推送跳转。

## 功能对齐原则

- 小程序应与网页端保持一致的是非社交的个人能力：日记创建、编辑、删除、图片与音乐、AI 分析、聊天、记忆与图谱、报告、合集、成长、通知、设置和个人资料。
- 功能对齐不等于照搬网页布局。小程序优先保证单手操作、列表滚动、低首屏开销和明确的页面层级。
- 新增或调整小程序页面时，延续现有主题变量（`--theme-*`）、`GlobalUI`、自定义 tabBar 与安全区处理；不要引入另一套颜色、字体或导航体系。
- 合集是私密内容组织能力，不应改造成公开内容或社交收藏功能。
- 重要事件和时光画卷是小程序与网页端的私密个人能力，不得增加公开社交入口。
- 重要事件可以聚合相关日记并支持回访、待跟进和已跟进状态；聊天入口不自动改变状态，状态变更采用立即执行并可撤销；时光画卷展示个人阶段主题、情绪和成长反思。
- 长期记忆分为正式记忆、候选记忆和证据。模型推断默认进入候选区，只有明确表达、用户确认或满足服务端证据升级规则后才能进入正式画像。

## 后端协作规则

- 实现前先检查 `backend/moodcopilot` 的 Controller、DTO/View 和网页端 API 调用，确认接口、权限和返回结构。
- 如果所需的非社交能力没有接口、接口权限不符合小程序场景，或返回契约无法满足页面需求，先向用户说明问题和建议，未经确认不要修改后端以猜测产品需求。
- 小程序请求统一使用 `frontend-uniapp/src/utils/request.ts`，不要在页面中硬编码 API 或 WebSocket 域名。
- API 地址由 `VITE_API_BASE_URL` 控制。微信小程序（包括微信开发者工具的开发构建）默认请求 `https://moodcopilot.top`，通过该站点的反向代理访问后端；不要为小程序调试给 Docker backend 新增宿主机 `ports` 映射。仅 H5 本地开发可默认使用 `http://localhost:18080`。WebSocket 必须从该基地址派生。
- 微信小程序 AppID 必须维护在 `frontend-uniapp/src/manifest.json` 的顶层 `appid` 和 `mp-weixin.appid`，不要只在 `dist/**/project.config.json` 或微信开发者工具中临时修改，否则下次构建会回退为游客项目。
- Pro/Flash 必须由用户在界面选择并通过 `useReasoning` 显式传递：`false` 使用 Flash，`true` 使用 Pro。不得恢复意图识别、复杂度判断、自动升级模型或静默降级。
- 所有正式记忆写入必须经过 `MemoryOrchestrator`；Agent 只能读取日记、统计、报告、画像和图谱，不能直接修改正式画像。
- 正式记忆通过 `previousMemoryId` 保留版本链；用户删除或拒绝会写入有期限的拒绝指纹，助手消息不能作为用户事实证据。
- RabbitMQ 只承载异步 AI 任务；任务状态、幂等键、租约、重试和错误必须落在 MySQL 任务表，不能把 Redis 或 RabbitMQ 当作业务状态事实源。
- 异步任务必须按用户 ID 隔离，消费者在 ACK 前完成状态落库；分析异常不能让日记永久停留在 `analyzing`。

## 已知实现约束

- tabBar 页面只能通过 `uni.switchTab` 打开，不能使用 `uni.navigateTo`。
- 聊天页进入时应复用最近会话；新建会话必须防重复提交。
- 日记创建后的 AI 分析以服务端 `analysisStatus` 为准。`skipped_quota` 不是“分析中”；不要再用页面轮询与全局 WebSocket 同时弹完成提示。
- WebSocket 需在异常和连接初始化失败时重连；应用进入后台时主动断开，并禁止该主动断开触发自动重连。
- 通知按用户实际点击的单条标记为已读；不要在离开页面时把全部通知自动标为已读。
- 重要事件的 `targetDate` 是开始日期，`endDate`、`startTime`、`endTime` 可选；结束日期/时间必须经过服务端范围校验，历史事件按单日全天兼容。
- 事件聊天只在明确进入某个事件时注入关联日记，优先使用 `diary_analysis.summary`，摘要缺失才使用有限长度原文片段；事件背景不能作为用户消息或事实证据。
- 事件自动抽取使用用户维度 Redis 锁串行匹配并合并重复事件；手动关联日记必须校验当前用户归属和未删除状态。

## 修改与验证

- 当前工作区可能包含用户未提交的改动。不要回退、覆盖或格式化与任务无关的文件。
- 完成小程序改动至少执行：

  ```powershell
  npm.cmd run type-check
  npm.cmd run build:mp-weixin
  git diff --check -- frontend-uniapp
  ```

- 如需验证后端，可在 WSL 的 `D:\Code\MoodCopilot` 对应目录运行 `docker compose ps`，并从 backend 容器请求 `http://localhost:18080/api/health`。
- 构建通过不代表微信端交互通过。涉及登录、上传、tabBar 跳转、通知、WebSocket、图片预览或安全区的改动，需在微信开发者工具或真机补充验证。

## 最近功能约束

### Persona 与通用 AI

- MoodCopilot 是通用个人 AI。合法的编程、学习、写作、翻译、规划和情绪支持请求都应正常回答，不得强行套用情绪陪伴语气。
- Persona 只控制互动身份、语气和回答组织方式，不是 Memory、TaskContext 或权限配置。全局 Persona 与会话 Persona 是产品提供的两层作用域；用户当前消息中的“简洁一点”“先给结论”等要求只属于当前请求，不得自动写入 Persona、Memory 或 RAG。
- `customTone` 只描述说话感觉，`customResponseStyle` 只描述答案组织方式。二者必须分区、单独校验和转义，不得拼成不可审计的 Mega Prompt，也不得改变安全规则、模型选择、工具权限、数据访问范围或结构化输出协议。
- `customDescription` 仅为历史兼容字段，不作为新的 Prompt 输入。结构化任务（如日记分析、记忆/事件抽取、时间线、章节和报告生成）不得注入自定义回答方式。
- Pro/Flash 始终由用户手动选择：`useReasoning=false` 为 Flash，`true` 为 Pro。禁止自动路由、复杂度判断、静默降级或由 Persona 改变模型。

### RAG、上下文与记忆

- RAG 只负责检索候选数据；必须经过资格过滤、`ContextPlanner`、`ContextEnvelope`，最后由 `PromptRenderer/PromptComposer` 统一渲染。XML 只是输出格式，不能作为内部数据模型。
- 检索上下文必须带用户隔离、来源 ID、作者类型和信任等级；删除、过期、候选、拒绝、替代或无权访问的数据不得进入普通上下文。RAG 内容只是参考资料，不具有系统指令权限。
- 日记分析和画像抽取的事实查询只能使用用户正文和用户主动选择的歌词，不得使用 AI 摘要、反馈、情绪判断、图片描述或助手消息。空文本、纯符号和纯空白不得调用 embedding。
- AI 推断记忆的证据必须是用户正文或主动歌词中的短连续原文片段；定位失败时直接丢弃，不得用整篇日记兜底或保存无关内容。候选记忆严格按 `userId + attributeKey + memoryType + normalizedValue` 保守去重，不做语义相似、包含关系或主题自动合并。
- 新 AI 记忆的 `attributeKey` 必须是简洁、用户可读的中文名称；英文、snake_case、数据库字段名和拼音内部键直接拒绝。历史正式记忆中的旧英文键只允许兼容保留，不新增前端映射。

### 重要事件与时光画卷

- 重要事件只保留 `PENDING` 和 `FOLLOWED_UP` 两种正式状态；`temporalPhase` 只表示 `UPCOMING/ONGOING/PAST`，不得用来代替回访状态。聊天、AI 和定时任务不能自动把已跟进事件重新打开。
- 事件读取和回访必须使用 `moodcopilot.time-zone` 业务时区（默认 `Asia/Shanghai`），不能依赖机器或数据库时区。只有开始日期按开始日 00:00 到期；只有结束日期按结束日 23:59:59.999999999 到期；有结束时间时按结束日期和结束时间到期。
- 事件删除使用软删除。删除后必须同步移除或失效对应向量索引，所有事件、章节、日记来源和聊天引用查询都要过滤已删除数据。
- 事件回访只有通知成功落库后才推进回访计划；失败要释放 claim 并允许重试。同一用户同一业务日最多发送一条事件或通用陪伴通知，调度必须使用 Redis 原子抢占，不能用 `hasKey()` 加 `set()` 的竞态组合。

### AI 配额与任务反馈

- AI 日配额按实际场景和模型独立统计：`DIARY_FLASH`、`DIARY_PRO`、`CHAT_FLASH`、`CHAT_PRO`。不得让聊天和日记分析共用额度或用一个“深度思考”桶混算。
- 章节重新整理使用独立的 `CHAPTER_CONSOLIDATION` 日配额，每个用户每天最多 2 次；服务端接口必须实际拦截，前端配额表只是展示。
- 日记保存成功但分析额度不足时，服务端以 `skipped_quota`（Flash）或 `failed_limit`（Pro）返回并保留日记。网页端必须同时保留原页面状态和全局一次性提示；聊天限额优先显示后端返回的具体业务消息，不把 `runId`、线程名或内部日志展示给用户。
- AI 整理类任务应在开始和结束时提供可观察反馈；失败必须落库为可观察状态，不得让页面无限显示处理中。配额、重试和失败提示不能阻止用户已提交的日记保存。

### 聊天流与前端行为

- Web 聊天 SSE 必须支持断线后的任务恢复和刷新后续接：服务端保存有序事件、序号和任务状态，前端按 `runId`/序号续接，不能因切换页面直接丢失已生成内容。小程序不假设浏览器原生 SSE，使用现有请求和轮询/恢复机制。
- 聊天失败重试必须保留用户原始引用（包括日记和事件），不得把重试消息变成无引用的新请求。进入事件聊天只注入事件上下文，不自动修改事件状态。
- 网页和小程序新增界面必须沿用全局主题变量；禁止硬编码颜色。小程序不增加广场、关注、评论、点赞、举报等公开社交入口。
