---
mode: agent
description: "MoodCopilot 自动工作流：按任务类型自动调用审查/E2E/文档子代理并汇总结果"
model: GPT-5 (copilot)
---
请使用 MoodCopilot 工作流编排代理处理以下任务，并自动调用合适子代理完成执行与汇总。

任务描述：${input:请描述要执行的需求、排障或同步任务}

执行要求：
1. 自动识别任务属于审查、E2E、文档同步中的哪一类或多类。
2. 必须调用对应子代理，不要跳过。
3. 若多类任务，按“审查 -> 测试 -> 文档同步”顺序执行。
4. 输出必须包含：路由决策、执行证据、阻塞项、最终结论与下一步建议。
