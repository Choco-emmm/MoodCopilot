package com.moodcopilot.ai;

/** The user's intent when attaching a reference to the current request. */
public enum ReferencePurpose {
    ANALYZE,
    QUOTE,
    RECALL,
    COMPARE,
    SUMMARIZE,
    DISCUSS;
}
